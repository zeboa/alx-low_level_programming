#include <stdio.h>

/**
 * main - prints lowercase alphabet
 * Return: 0 if exited propely, non zero otherwise
 */

int main(void)
{
	int n;
	
	for (n = 11; n = 21; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
