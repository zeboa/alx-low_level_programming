#include <stdio.h>

/**
 * main - Print all single digits in base 10
 * Returnn: 0 if executed properly
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");

	return (0);
}
