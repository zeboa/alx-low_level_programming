#include "main.h"

/**
 * print_numbers -  prints the numbers, from 0 to 9
 * @n: int to be printed
 * Return: multiplication result
 */

void print_numbers(void);

	int n;
		
{
	for(n >= 0, n = 9, n++)
		return (n);
}
